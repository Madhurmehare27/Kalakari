"use client"

function galleryLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      <header>
        
      </header>
      <main>
        {children}
      </main>
    </div>
  );
}

export default galleryLayout;